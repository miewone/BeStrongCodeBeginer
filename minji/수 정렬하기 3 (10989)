# N개의 수가 주어졌을 때, 이를 오름차순으로 정렬하는 프로그램을 작성하시오.

import sys

N = int(sys.stdin.readline())    # input(), append(), sort() 사용 시 runtime error 발생 
lst = [0] * 10001
for i in range(N): 
    num = int(sys.stdin.readline())
    lst[num] += 1                       # lst[input 받은 값]의 개수를 + 1 
for j in range(10001):
    if lst[j] != 0:
        for n in range(lst[j]):
            print(j)
